package ui

import "fmt"

templ LeaderBoard(Names []string, Scores []float32) {
	<div class="flex flex-col items-center w-full max-w-md mx-auto animate-in fade-in duration-500">
		if len(Names) == 0 || len(Names) != len(Scores) {
			<div class="bg-slate-800 border border-dashed border-slate-700 rounded-2xl p-12 text-center w-full">
				<p class="text-slate-500 italic">まだデータがありません</p>
				<p class="text-slate-600 text-sm mt-2">(No active users yet)</p>
			</div>
		} else {
			<div class="text-center mb-10">
				<div class="bg-slate-800 border border-slate-700/50 rounded-[2rem] p-8 mb-6 shadow-xl">
					<h2 class="text-3xl font-black text-amber-500 tracking-[0.2em] uppercase">
						酔っぱらいランキング
					</h2>
				</div>
				<div class="relative flex flex-col items-center w-full bg-gradient-to-br from-indigo-950 to-slate-900 border-2 border-amber-500 rounded-[3rem] p-10 mb-10 shadow-[0_20px_50px_rgba(245,158,11,0.15)] overflow-visible">
					<div class="absolute -top-8 -right-4 transform rotate-12 drop-shadow-2xl">
						<span class="text-7xl">👑</span>
					</div>
					<div class="relative mb-8">
						<div class="p-1.5 bg-gradient-to-b from-amber-400 to-amber-700 rounded-[2rem] shadow-2xl">
							<img
								src="/assets/yopparai.png"
								class="w-40 h-40 rounded-[1.8rem] object-cover border-4 border-slate-900"
							/>
						</div>
						<div class="absolute -bottom-4 left-1/2 -translate-x-1/2 bg-amber-500 text-slate-900 font-black px-6 py-1.5 rounded-full text-lg shadow-xl border-4 border-slate-900 whitespace-nowrap">
							1位
						</div>
					</div>
					<div class="flex flex-col items-center space-y-2 mt-4">
						<p class="text-amber-500/70 text-xl uppercase tracking-[0.3em] font-bold">
							一番酔っ払いやつ
						</p>
						<h3 class="text-5xl font-black text-white leading-tight">
							{ Names[0] }
						</h3>
						<div class="mt-4 px-8 py-2 bg-amber-500/10 border border-amber-500/30 rounded-2xl">
							<p class="text-amber-400 font-mono text-3xl font-bold">
								{ fmt.Sprintf("%.1f pts", Scores[0]) }
							</p>
						</div>
					</div>
				</div>
			</div>
			<ul class="w-full space-y-3">
				for idx, name := range Names {
					if idx != 0 {
						<li class="flex items-center justify-between bg-slate-800/50 hover:bg-slate-800 border border-slate-700/50 p-4 rounded-xl transition-all duration-200 group">
							<div class="flex items-center space-x-4">
								<span class="w-8 text-slate-500 font-mono font-bold group-hover:text-indigo-400">
									{ fmt.Sprintf("#%02d", idx + 1) }
								</span>
								<span class="text-slate-200 font-medium">{ name }</span>
							</div>
							<span class="text-indigo-400 font-mono font-semibold">
								{ fmt.Sprintf("%.1f",Scores[idx]) }
							</span>
						</li>
					}
				}
			</ul>
			<button
				hx-get="/"
				hx-target="#main-area"
				class="mt-8 text-slate-500 hover:text-white text-sm transition-colors flex items-center space-x-2"
			>
				<span>← 戻る (Back)</span>
			</button>
		}
	</div>
}
